import { AppLayout } from "@/components/layouts/app"

export const metadata = {
  title: "Términos y condiciones | codelabzgz",
  description: ""
}

export default function Layout({ children }) {
  return <AppLayout>{children}</AppLayout>
}
