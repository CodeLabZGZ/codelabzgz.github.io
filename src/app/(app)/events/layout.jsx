import { AppLayout } from "@/components/layouts/app"

export const metadata = {
  title: "Eventos | codelabzgz",
  description: ""
}

export default function Layout({ children }) {
  return <AppLayout>{children}</AppLayout>
}
